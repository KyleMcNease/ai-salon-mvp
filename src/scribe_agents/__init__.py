"""
Agent-facing primitives for the Scribe runtime.

The runtime layer builds on top of ``scribe_core`` provider clients to deliver
agent orchestration (tool routing, memory hooks, etc.).
"""

__all__ = ["runtime"]
